class Thing {
    getCallback() {
        func localFunction() {
            print "self is: " + self;
        }

        return localFunction;
    }
}

let callback = Thing().getCallback();
callback();